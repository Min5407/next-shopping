import { FastForward, ShieldCheck, PersonStanding } from "lucide-react";
import Logo from "./logo";

export const ICONS = {
  SHIELD_CHECK: ShieldCheck,
  PERSON_STANDING: PersonStanding,
  FAST_FORWARD: FastForward,
  LOGO: Logo,
};
